<%= form_for(@post)  do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <h1><%= f.text_field :title, :placeholder => "Title", style: "width: 25%%;" %> By: <%= f.select :author, options_for_select([["Jesse Mills"],["Other"]]), style: "width: 25%;"%></h1></br>
    <%= f.text_area :body, :placeholder => "Body", style: "width: 85%; min-height: 300px;"%></br>
    <%= f.submit name: nil%>
  </div>
<% end %>
